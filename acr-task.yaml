version: v1.1.0
stepTimeout: 300

steps:
  - cmd: docker run mcr.microsoft.com/azure-cli echo "ACR Task Started"
    id: step1

  - cmd: docker run mcr.microsoft.com/azure-cli sleep 5
    id: step2

  - cmd: docker run mcr.microsoft.com/azure-cli echo "ACR Task Completed Successfully"
    id: step3
